# Round 9: 图片上传压缩

本轮只改了两处：
- `components/CreatePostModal.tsx`
- `lib/image-compression.ts`

## 行为变化
- 发帖时，选中的图片会先在浏览器本地压缩，再上传到 Supabase Storage。
- 默认压缩参数：
  - 最大边长：`1920`
  - 质量：`0.82`
  - 小于约 `350KB` 且尺寸已经不大的图片，会直接保留原图。
- `GIF` / `SVG` 默认跳过压缩，保持原样上传。
- 普通位图会优先转为 `webp` 再上传，以降低体积。

## UI变化
- 发布按钮上传中状态从 `发射中...` 改成 `压缩并上传中...`
- 发帖弹窗底部增加了一行提示文案，说明会自动压缩。

## 不需要动的部分
- 不需要改数据库
- 不需要改 `threads` 表
- 不需要改图片展示逻辑

## 覆盖后建议
执行：

```bash
cd ~/threads-clone
rm -rf .next
npm run dev
```

然后用一张大图测试：
- 上传前查看本地原图大小
- 上传后在浏览器网络面板里看最终请求和 Storage 文件大小
